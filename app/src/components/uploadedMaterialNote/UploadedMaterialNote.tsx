import React, { type FunctionComponent } from "react";

import * as styles from "./UploadedMaterialNote.styles";

interface UploadedMaterialNoteProps
{

}

const UploadedMaterialNote: FunctionComponent<UploadedMaterialNoteProps> = ({  }) => {
  return (
    <div css={styles.wrapper}>
      UploadedMaterialNote
    </div>
  );
};

export default UploadedMaterialNote;
