import React, { type FunctionComponent } from "react";

import * as styles from "./CaseProgressCard.styles";

interface CaseProgressCardProps
{
  
}

const CaseProgressCard: FunctionComponent<CaseProgressCardProps> = ({  }) => {
  return (
    <div css={styles.wrapper}>
      CaseProgressCard
    </div>
  );
};

export default CaseProgressCard;
