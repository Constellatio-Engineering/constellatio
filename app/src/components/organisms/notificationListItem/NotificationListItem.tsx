import React, { type FunctionComponent } from "react";

import * as styles from "./NotificationListItem.styles";

const NotificationListItem: FunctionComponent = () => (
  <div css={styles.wrapper}>

  </div>
);

export default NotificationListItem;
